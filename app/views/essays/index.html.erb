
<table>
  <tr>
    <th>Url_Title</th>
    <th>Content</th>
  </tr>

  <% @essays.each do |essay| %>
    <tr style="<%= essay.is_private? ? 'color:red; font-weight:bold;' : ''  %>">
      <td><%=h essay.url_title %></td>
      <td><%=h essay.content %></td>
      <td><%= link_to "Show", '/essays/' + essay.url_title %>       </td>
      <td><%= link_to 'Edit', essay_edit_path(essay.url_title) %></td>
      <!-- <td><%= link_to 'Destroy', essay, :confirm => 'Are you sure?', :method => :delete %></td> -->
      <!-- <td><%= link_to 'Destroy', '/essays/' + essay.url_title, :method => :delete%></td> -->
    </tr>
  <% end %>
</table>

<br />

<!-- <%= link_to 'New essay', new_essay_path %> -->
